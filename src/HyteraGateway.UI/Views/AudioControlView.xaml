<UserControl x:Class="HyteraGateway.UI.Views.AudioControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <materialDesign:Card Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="Headphones" Width="24" Height="24" VerticalAlignment="Center"/>
                <TextBlock Text="Audio Control" Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                           Margin="8,0,0,0" VerticalAlignment="Center"/>
                <Ellipse Width="12" Height="12" Margin="16,0,0,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="Gray"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Fill" Value="Green"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsTransmitting}" Value="True">
                                    <Setter Property="Fill" Value="Red"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsReceiving}" Value="True">
                                    <Setter Property="Fill" Value="Orange"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding AudioState}" Margin="8,0,0,0" VerticalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
            
            <!-- Connection & Selection -->
            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" Margin="0,0,8,0"
                         materialDesign:HintAssist.Hint="Radio ID"
                         Text="{Binding SelectedRadioId, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBox Grid.Column="1" Margin="0,0,8,0"
                         materialDesign:HintAssist.Hint="TalkGroup ID"
                         Text="{Binding SelectedTalkGroupId, UpdateSourceTrigger=PropertyChanged}"/>
                
                <Button Grid.Column="2" Command="{Binding ConnectCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Content="{Binding IsConnected, Converter={StaticResource BoolToConnectTextConverter}, FallbackValue=Connect}"/>
            </Grid>
            
            <!-- PTT Button -->
            <Button Grid.Row="2" Height="80" Margin="0,0,0,16"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{Binding IsTransmitting, Converter={StaticResource BoolToPttColorConverter}, FallbackValue=DarkRed}"
                    PreviewMouseLeftButtonDown="PttButton_MouseDown"
                    PreviewMouseLeftButtonUp="PttButton_MouseUp">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Microphone" Width="32" Height="32" HorizontalAlignment="Center"/>
                    <TextBlock Text="PUSH TO TALK" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <TextBlock Text="(Hold to transmit)" HorizontalAlignment="Center" FontSize="10" Opacity="0.7"/>
                </StackPanel>
            </Button>
            
            <!-- Volume & Audio Level -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Command="{Binding ToggleMuteCommand}"
                        Style="{StaticResource MaterialDesignIconButton}">
                    <materialDesign:PackIcon>
                        <materialDesign:PackIcon.Style>
                            <Style TargetType="materialDesign:PackIcon">
                                <Setter Property="Kind" Value="VolumeHigh"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMuted}" Value="True">
                                        <Setter Property="Kind" Value="VolumeOff"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </materialDesign:PackIcon.Style>
                    </materialDesign:PackIcon>
                </Button>
                
                <Slider Grid.Column="1" Minimum="0" Maximum="1" 
                        Value="{Binding Volume}" Margin="8,0"/>
                
                <!-- Audio Level Meter -->
                <ProgressBar Grid.Column="2" Width="100" Height="16"
                             Minimum="0" Maximum="1" Value="{Binding AudioLevel, Mode=OneWay}"
                             Foreground="Green"/>
            </Grid>
        </Grid>
    </materialDesign:Card>
</UserControl>
